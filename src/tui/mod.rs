//! TUI (Terminal User Interface) main module.
//!
//! Implements the interactive directory selector using ratatui and crossterm.
//! Manages the event loop, terminal setup/teardown (raw mode, alt screen on stderr),
//! and coordinates rendering, input handling, and dialog state.
