//! Command implementations for the lab CLI.
//!
//! Each subcommand (init, install, clone, worktree, exec, cd)
//! is implemented in its own submodule and dispatched from main.

pub mod clone;
pub mod init;
pub mod install;
